/**
 * Display the message on the STDOUT.
 * @param {String} msg The message to display.
 * @author Isaaac Kumatse <https://github.com/qwertyriks>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
